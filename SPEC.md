# SPEC — 사용자 시나리오

이 저장소가 무엇을 보장하는지를 "누가 무엇을 하려 할 때 무엇이 일어나는가"로 적는다.
설계 근거와 배경은 [README](./README.md) 에 있다. 여기는 **확인 가능한 동작**만 남긴다.

각 시나리오는 셋으로 적는다.

- **상황** — 사용자가 처한 자리와 하려는 것
- **동작** — 시스템이 하는 일
- **확인** — 실제로 그런지 볼 수 있는 방법. 여기가 어긋나면 버그다

## 액터

| | 하는 일 | 접점 |
| --- | --- | --- |
| **읽는 사람** | 예전에 눌러둔 별표를 다시 찾는다 | `web/` — 브라우저 |
| **관리하는 사람** | 스냅샷을 최신으로 유지한다 | `core/` — CLI, `sqlite3` |

둘 다 같은 사람일 수 있다. 그래도 나누는 이유는 접점이 다르고, 한쪽이 멈춰도 다른
쪽은 돌기 때문이다. **web 은 커밋된 `stars.db` 만 읽는다.** 적재가 몇 달 밀려도
읽기는 그대로 동작하고, 반대로 web 이 없어도 적재는 완결된다.

---

# A. 읽는 사람

전제: `pnpm --filter @stars/web dev` 또는 배포본. 로그인·설정·계정 개념이 없다.

## A1. 컬렉션의 모양을 본다

**상황** — 처음 연다. 무엇이 얼마나 있는지 모른다.

**동작** — 머리말이 별표 총계를 낸다. 그 아래 스펙트럼 막대가 **언어** 비중을 폭으로
그린다. 언어는 GitHub 이 정하고 repo 당 0 또는 1개라, 막대는 컬렉션을 실제로 나눈
것이다. 본문에는 최근에 누른 순으로 상위 120개가 깔린다.

**확인**

- 세그먼트가 열 개다. 저장소 수 상위 9개 언어에 나머지를 합친 `기타` 하나
- 폭이 그 언어의 저장소 수에 비례한다. TypeScript 971 · JavaScript 755 로 둘이 대부분
- 카드가 120개, 머리말 수치와 `개 일치` 수치가 모두 `2,344`
- `상위 120개 표시`가 함께 뜬다 (일치가 120을 넘을 때만)

## A2. 이름·설명·저자로 찾는다

**상황** — "sqlite 관련해서 뭘 별표했더라"

**동작** — 입력이 멎고 220ms 뒤 URL 의 `q` 가 갱신되고 서버가 다시 그린다. 저장소
전체 이름·설명·소유자 세 곳을 부분 일치로 본다. 대소문자를 가리지 않는다.

**확인**

- `?q=sqlite` → `12개 일치`
- 타이핑 중에는 요청이 나가지 않는다 (멈춘 뒤 한 번)
- 입력칸의 `×` 로 지우면 검색이 풀린다

## A3. 토픽으로 좁힌다

**상황** — 리액트 관련된 것만 보고 싶다.

**동작** — 칩을 누르면 `topic` 이 걸린다. 칩은 **GitHub 에 실제로 달려 있는 토픽**이고
우리가 만든 이름이 아니다. 저장소 수 상위 20개를 내보내되, 언어 필터와 이름이 같은
토픽(`javascript`, `typescript`, `css` …)은 뺀다. 같은 것을 다시 누르면 풀린다.

**확인**

- `?topic=react` → `393개 일치`, 칩이 `react · 393`
- **칩의 숫자와 누른 결과 수가 같다.** 토픽 하나가 곧 필터 하나다
- 칩 줄에 `javascript` 나 `typescript` 가 없다. 바로 옆 언어 셀렉터와 겹치기 때문이다
- 칩이 가로를 넘치면 `+N` 드롭다운으로 접힌다
- `react` 와 `reactjs` 가 **둘 다 뜬다.** GitHub 에 둘 다 달려 있고, 합치는 건 판단이라 하지 않는다

## A4. 언어로 좁힌다

**상황** — Rust 로 된 것만.

**동작** — 셀렉터에서 고르거나 **스펙트럼 세그먼트를 눌러서** `lang` 을 건다. 둘은 같은
파라미터를 쓴다. 다른 필터와 **곱해진다**.

**확인**

- `?lang=Rust` → `86개 일치`, `?topic=cli&lang=Go` → `7개 일치`
- 세그먼트를 누르면 셀렉터 표시값도 따라 바뀐다
- `기타` 세그먼트는 눌리지 않는다. "그 외 전부"는 볼 만한 묶음이 아니다
- 셀렉터에는 **상위 20개 언어만** 뜬다. 그보다 드문 언어는 고를 수 없다 (A10 참고)
- 언어가 없는 저장소 93개는 어떤 언어로도 걸러지지 않는다

## A5. 정렬을 바꾼다

**상황** — 기본은 최근에 누른 순이다. 별표 목록은 "요즘 뭘 봤나"로 여는 게
맞고, 인기순은 그 다음 질문이다. 인기순이나 이름순으로 바꾼다.

**동작** — 최근 star순(기본) · 스타순 · 이름순 셋. 정렬은 좁힌 결과 안에서 돈다.

**확인**

| 정렬 | 첫 카드 | URL |
| --- | --- | --- |
| 최근 star순 (기본) | `shadcn-ui/cn` | 파라미터 없음 |
| 스타순 | `codecrafters-io/build-your-own-x` | `?sort=stars` |
| 이름순 | `0xGF/boneyard` | `?sort=name` |

기본값은 URL 에 남기지 않는다. 셀렉터의 첫 항목이 곧 기본값이다
(`web/app/controls.tsx` 의 `SORTS`, `DEFAULT_SORT`) — 순서를 바꾸면 기본값도
따라 바뀐다.

## A6. 아무것도 안 나온다

**상황** — 너무 좁혔다.

**동작** — 카드 자리에 `조건에 맞는 저장소가 없어요` 와 `필터 초기화` 버튼이 온다.

**확인** — `?q=zzzznope` 에서 뜬다. 버튼을 누르면 `/` 로 가고 모든 필터가 풀린다.

## A7. 저장소로 넘어간다

**상황** — 찾았다. 원본을 열고 싶다.

**동작** — 카드 전체가 링크다. 새 탭에서 GitHub 를 연다.

**확인** — 카드 하나가 보여주는 것

- `owner/name` — 소유자는 흐리게, 이름은 진하게
- 스타 수. 1,000 이상은 `545k` 처럼 줄인다
- 설명 최대 세 줄. 없으면 `설명 없음`
- 언어 토큰. **색이 스펙트럼의 그 언어 칸과 같다** — 파이썬 칸이 시안이면 파이썬 카드도 시안이다
- 토픽 토큰 최대 네 개. GitHub 에 달린 그대로이고 무채색이다. 언어와 이름이 겹치는 토픽은 뺀다
- `archived` 배지 (보관된 저장소만)

## A8. 본 화면을 그대로 다시 연다

**상황** — 링크를 남겨두거나 새로고침한다.

**동작** — 상태는 전부 URL 에 있다: `q` `lang` `topic` `sort`. 클라이언트는 URL 만
갱신하고 데이터는 서버가 다시 읽는다. 브라우저 히스토리를 늘리지 않는다(`replace`).

**확인** — 주소를 복사해 새 창에 붙이면 검색어·필터·정렬이 그대로 복원된다.

## A9. 어두운 화면에서 본다

**동작** — OS 설정을 따른다. 색은 전부 테마 토큰이라 라이트/다크가 함께 정의된다.

**확인** — OS 를 다크로 바꾸면 배경·카드·토큰·스펙트럼이 모두 따라간다. 앱이 자체로
가진 색 값은 없다.

## A10. 여기서 할 수 없는 것

명시적으로 범위 밖이다. 필요해지면 그때 넣는다.

- **페이지 넘기기가 없다.** 한 화면에 120개까지다. 그 이상은 좁혀서 본다
- **드문 언어로 못 거른다.** 셀렉터는 상위 20개만 담는다
- **뺀 별표를 못 본다.** web 은 현재 별표만 읽는다. 뺀 것은 `unstarred` 뷰에 남아 있고 `sqlite3` 로 본다 (B9)
- **README 본문 검색이 없다.** 목록 API 가 설명과 토픽까지만 주기 때문이다
- **토픽이 없는 저장소는 칩으로 안 잡힌다.** 2,344개 중 652개(27.8%)가 토픽이 하나도 없다. 검색과 언어로 찾는다
- **드문 토픽으로 못 거른다.** 4,788개 중 상위 20개만 칩이 된다
- **우리가 만든 도메인 분류는 화면에 없다.** 정의는 남아 있고 `sqlite3` 로 질의한다 (B6, B9)

---

# B. 관리하는 사람

전제: Node 24, `GITHUB_TOKEN`/`GH_TOKEN` 또는 `gh auth login`. 필요한 권한은
`starring:read` 하나다.

## B1. 처음 적재한다

**상황** — 빈 DB.

**동작** — `pnpm --filter @stars/core sync --full`. 목록 전체를 `direction=asc` 로
훑는다. DB 가 비어 있으면 `--full` 없이도 전체로 돈다.

**확인** — 끝에 `동기화 #1 (full) 본 것 N / 신규 N / 변경 0 / 언스타 0` 과
`현재 별표 N` 이 찍힌다. 별표 2,344개 기준 24요청·60초.

## B2. 이후 갱신한다

**상황** — 며칠 뒤. 새로 누른 것만 넣고 싶다.

**동작** — `pnpm --filter @stars/core sync`. `direction=desc` 로 앞부분만 보고,
가진 것 중 가장 최근 별표 시각에 닿으면 멈춘다. 경계 값은 다시 넣는다 — 같은 초에
별표가 둘 걸린 경우를 놓치지 않기 위해서고, 재적재는 멱등이라 비용이 없다.

**확인**

- 보통 1요청·2.5초로 끝난다
- 새 별표가 없으면 `신규 0 / 변경 N` — `변경` 은 스타 수·푸시 시각이 움직인 것이다
- **`언스타` 가 찍히지 않는다.** 증분은 언스타를 판정하지 않는다 (B3)

## B3. 별표를 뺀다

**상황** — GitHub 에서 별표를 해제했다.

**동작** — row 는 지우지 않고 `unstarred_sync` 에 알아챈 동기화 번호를 남긴다.
판정은 **목록 전체를 본 동기화에서만** 한다. 증분에서 판정하면 아직 못 본 저장소가
전부 오탐이 되기 때문이다.

**확인**

- `sync --full` 뒤에만 `언스타 N` 이 찍힌다
- `SELECT * FROM unstarred` 에 뺀 시각과 알아챈 시각이 남는다
- web 목록에서 사라진다
- 다시 별표하면 되살아나고 **`first_sync` 는 보존된다.** `starred_at` 만 새 값이 된다

## B4. 동기화가 중간에 끊긴다

**상황** — 24페이지 중 17페이지에서 네트워크가 끊겼다.

**동작** — 페이지 하나가 트랜잭션 하나다. 앞 16페이지는 남고 그 동기화는 `ok=0` 으로
끝난다. `ok=0` 인 동기화는 언스타를 판정하지 않는다.

**확인**

- `동기화 #N 중단: <이유>` 가 stderr 로 나가고 종료 코드가 `1`
- 반쪽 페이지가 남지 않는다
- **언스타 오탐이 없다.** 다시 돌리면 이어진다

## B5. 레이트 리밋에 걸린다

**동작** — 남은 요청이 0이면 복구 시각을 계산해서 알려준다.

**확인** — `레이트 리밋 소진. 14:32:10 에 복구 (18분 후)`. 시간당 5,000요청에 전체
동기화가 24요청이라 실제로 닿기는 어렵다.

## B6. 분류 규칙을 고친다 (분석용)

**상황** — 분류가 있어야 답할 수 있는 질문이 생겼다. **화면은 이 분류를 쓰지 않는다**
(「분류를 왜 안 만들었나」 참고). `sqlite3` 로 질의할 때만 쓰는 도구다.

**동작** — `core/src/categories.ts` **하나만** 고치고 `node core/make-views.ts ./stars.db`
를 돌린다. 스크립트가 `core/sql/views.sql` 의 생성 구간을 다시 찍고 그 db 에 적용한
뒤 `repo_category` 표를 통째로 다시 채운다.

**확인**

- `views.sql 갱신` 과 `stars.db 에 적용 — 분류 N건` 이 찍힌다
- **재동기화가 필요 없다.** 적재는 건드리지 않는다
- 배열 순서가 우선순위다. 좁은 도메인이 앞, 넓은 도메인이 뒤 — `ui` 를 앞에 두면 react 를 쓰는 차트 라이브러리까지 전부 `ui` 가 된다
- 키워드는 소문자·숫자·공백만 받는다. 그 외는 던진다
- 고쳐도 **web 화면은 달라지지 않는다.** 화면은 언어와 토픽만 읽는다

## B7. 스냅샷을 커밋한다

**동작** — 순서가 중요하다.

```bash
pnpm --filter @stars/core sync
node core/make-views.ts ./stars.db          # 분석용 분류에 새 repo 반영
sqlite3 stars.db 'PRAGMA wal_checkpoint(TRUNCATE); VACUUM;'
git commit -am "stars 스냅샷 갱신"
```

**확인**

- `stars.db-wal` 과 `-shm` 이 남아 있지 않다. **접지 않고 커밋하면 마지막 동기화가 빠진 파일이 올라간다** — web 은 `immutable=1` 로 열어서 WAL 을 읽지 않는다
- `make-views` 를 건너뛰어도 **화면은 멀쩡하다.** `repo_category` 만 뒤처진다 (B6)

## B8. 네트워크 없이 확인한다

**동작** — 합성 목록으로 파이프라인을 돌린다.

```bash
cd core
node make-fixture.ts
node src/cli.ts test.db --from fixture.json
```

`--from` 은 파일이 목록 전체라고 보므로 전체 동기화로 취급한다. 항목을 지운 파일로
다시 돌리면 언스타 판정까지 확인된다.

**확인** — `jq '.[0:3]' fixture.json > less.json` 뒤 재실행하면 `언스타 2`, 원본으로
되돌리면 `신규 2` 이고 `first_sync` 가 보존된다.

## B9. DB 를 직접 질의한다

**상황** — 화면이 답하지 않는 걸 묻는다.

**동작** — `sqlite3 stars.db` 로 뷰를 그대로 쓴다.

| 뷰 / 표 | 무엇 |
| --- | --- |
| `starred` | 현재 별표 |
| `unstarred` | 뺀 별표 + 알아챈 시각 |
| `topic` | GitHub 토픽을 편 것 |
| `repo_text` | 분류용 검색 문자열 |
| `repo_match` | 분류 규칙 (느리다, 정의용) |
| `repo_category` | 굳힌 분류 (표, 인덱스 있음) |
| `repo_primary_category` | 대표 도메인 |

**확인** — `sqlite3` CLI 로 스키마가 읽혀야 한다. 뷰 정의가 파서 스택을 넘기면
(`parser stack overflow`) node:sqlite 는 넘어가도 CLI 는 통째로 막힌다. 이 조건은
회귀하기 쉬우므로 `make-views.ts` 를 고칠 때마다 아래를 돌린다.

```bash
sqlite3 stars.db "SELECT name FROM sqlite_master WHERE type IN ('view','table')"
```

---

# 성능 기준

넘으면 회귀로 본다.

| | 기준 | 근거 |
| --- | --- | --- |
| 페이지 응답 | 50ms 안쪽 | 실측 20–33ms |
| 분류 집계 질의 (분석용) | 10ms 안쪽 | 굳힌 표 3ms. 규칙 뷰로 직접 재면 2초 |
| 전체 동기화 | 24요청 / 60초 | 별표 2,344개 기준 |
| 증분 동기화 | 1요청 / 2.5초 | |
| `stars.db` | 5MB 안쪽 | 현재 4.70MB |

분류 집계가 느려졌다면 `repo_category` 가 표가 아니라 뷰로 돌아갔는지 먼저 본다.

# 분류를 왜 안 만들었나

화면은 **GitHub 이 갖고 있는 값만** 쓴다. 언어와 토픽이다. 도메인을 지어내 붙이지 않는다.

한번 지어내 봤고, 데이터가 그게 안 된다고 말했다.

| | 커버리지 | repo 당 개수 | 출처 |
| --- | --- | --- | --- |
| 지어낸 대표 도메인 | 76.6% | 겹침 — 45%가 2개 이상 | 우리 추측 |
| 언어 | 96.0% | 0 또는 1 | GitHub |
| 토픽 (상위 20개 칩) | — | 여러 개 | GitHub |

- **빠뜨린 도메인이 보이지 않았다.** 분류 안 된 548개에 언어·런타임(`golang/go`, `TypeScript`, `deno`), 유틸리티(`lodash`, `date-fns`, `zod`), 데브옵스(`act`, `sentry`)가 통째로 들어 있었다. 이름 붙인 것만 보이므로 빠진 건 끝까지 안 보인다
- **시간이 지나며 낡았다.** 2016~2020년 별표는 약 80%가 걸리는데 2026년 별표는 70.9%다
- **물량이 많은 자리의 정밀도가 나빴다.** `native` 가 `micromatch` 를, `api` 가 Web Audio 튜토리얼을 잡았다. 둘 다 각 도메인의 30%를 차지하던 키워드다
- **키워드 288개 중 15개는 한 번도 안 걸렸다.** `langchain`, `emacs`, `jasmine` 처럼 그럴듯해서 적었지만 이 컬렉션에 없는 것들이다
- **틀린 걸 알아챌 방법이 없었다.** 어떤 저장소가 잘못 들어갔는지 알려주는 게 아무것도 없다

애매한 저장소를 어디에 넣을지는 사람이 사흘을 봐도 쉽게 정하지 못한다. 그런 판단을
미리 굳혀 두고 그게 맞는지 확인할 길까지 없으면, 남는 건 측정값처럼 보이는 추측이다.
GitHub 의 값은 우리 것이 아니지만 적어도 우리가 지어낸 것이 아니다.

정의는 지우지 않았다. `core/src/categories.ts` 와 `repo_category` 로 그대로 남아 있고
`sqlite3` 로 질의할 수 있다(B6, B9). 분류가 있어야 답할 수 있는 질문이 생기면 그때
그 자리에서 만든다 — 미리가 아니라.

# 미결

- **README 본문 수집** — 전문 검색이 되지만 저장소당 요청 1회에 FTS5 인덱스가 붙는다. 적재라기보다 수집이라 범위를 정하지 않았다
- **삭제·비공개 전환** — 지금은 언스타와 구분되지 않는다. `/repositories/{id}` 로 갈라낼 수 있지만 건수가 쌓이기 전에는 근거가 없다
- **`gone` 오탐 창** — 전체 동기화 도중 별표를 빼면 무관한 저장소 하나가 그 회차에 언스타로 잡힌다. 다음 동기화에서 자동 복구되지만 `unstarred` 뷰에 잠깐 뜬다
- **페이지네이션** — 120개 상한을 넘겨 볼 방법이 없다 (A10)
