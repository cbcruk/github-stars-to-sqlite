{
  "name": "github-stars-to-sqlite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "pnpm --filter @stars/core sync",
    "dev": "pnpm --filter @stars/web dev",
    "build": "pnpm --filter @stars/web build"
  }
}
